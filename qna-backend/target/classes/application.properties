# -------------------------------
# ? Server Config
# -------------------------------
server.port=8080
spring.application.name=qna-backend

# -------------------------------
# ? MongoDB Connection
# -------------------------------
spring.data.mongodb.uri=mongodb+srv://sonal:1234@qna-backend.nb1rmkf.mongodb.net/qna-backend?retryWrites=true&w=majority&appName=qna-backend
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG

# -------------------------------
# ? JWT Configuration
# -------------------------------
# JWT
jwt.secret=your_super_secret_key_which_should_be_long_enough
jwt.expirationMs=86400000
# -------------------------------
# ? Dev Tools
# -------------------------------
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# -------------------------------
# ? Logging
# -------------------------------
logging.level.root=INFO
logging.level.com.qnabounty=DEBUG

# -------------------------------
# ? File Uploads
# -------------------------------
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB
